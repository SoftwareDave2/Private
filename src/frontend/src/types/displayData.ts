export type DisplayData = {
    macAddress: string,
    id: number,
    brand: string,
    model: string,
    width: number,
    height: number,
    orientation: string,
    filename: string,
    wakeTime: string,
}