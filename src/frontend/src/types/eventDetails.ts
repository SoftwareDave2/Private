export type EventDetails = {
    id: string;
    title: string;
    date: string;
    start: string;
    end: string;
    allDay: boolean;
    image: string; // Typ für das Bild (Base64 oder URL)
    macaddress: string
};